//const numbers = [1, 2, 3, 4, 5]

//const twice = numbers.map((batata) => batata * 2)

//console.log(twice)

const users = [
    { id: 1, name: 'Bruno' },
    { id: 2, name: 'Lucas' },
    { id: 3, name: 'Marcos' }
]

console.log(users.map((user) => user.id))